from setuptools import setup, find_packages

setup(
  name='rele',
  version='0.0.1',
  description='An inout mapping definition module.',
  url='https://github.com/xyla-io/rele',
  author='Xyla',
  author_email='access@xyla.io',
  license='MIT',
  packages=find_packages(),
  install_requires=[
  ],
  zip_safe=False
)